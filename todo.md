# บันทึกการทำงาน Sentiment API
อัปเดตล่าสุด: 7 เมษายน 2568, 03:45

## ✅ สิ่งที่ทำไปแล้ว

### แก้ไขปัญหาในโค้ดเพื่อให้รันใน localhost ได้ (03:35-03:40)

1. **แก้ไขโมเดล (03:35)**
   - แก้ไขไฟล์ `app/models.py` โดยเปลี่ยนฟิลด์เพิ่มเติม (user_id, platform ฯลฯ) ให้เป็น comment 
   - เหตุผล: เกิดข้อผิดพลาด `column sentiment_history.user_id does not exist` เนื่องจากมีการเพิ่มฟิลด์ในโมเดลแต่ฐานข้อมูลยังไม่ได้อัปเดต

2. **แก้ไข Controller (03:36)**
   - แก้ไขไฟล์ `app/main.py` ในส่วน dashboard เพื่อรองรับกรณีที่ไม่มีฟิลด์เพิ่มเติมในฐานข้อมูล
   - เพิ่ม try-except สำหรับจัดการกรณีที่เกิดข้อผิดพลาดเกี่ยวกับคอลัมน์ที่ไม่มี

3. **แก้ไข Template (03:37)**
   - แก้ไขไฟล์ `app/templates/dashboard.html` ให้ทำงานกับรูปแบบข้อมูลใหม่
   - เปลี่ยนจากการใช้ `stats.var` เป็น `var` โดยตรง

4. **เพิ่ม Setup Database Endpoint (03:38-03:40)**
   - เพิ่ม endpoint `/setup-db` ใน `app/main.py` สำหรับรัน migrations
   - ฟังก์ชันนี้จะสร้างตารางและเพิ่มคอลัมน์ที่จำเป็นถ้ายังไม่มี

### ตั้งค่าโครงสร้างเพื่อการ Deploy (03:30-03:35)

1. **Docker Compose (03:30-03:31)**
   - แก้ไข `docker-compose.yml` ให้ใช้งานพอร์ตและชื่อ containers ที่ไม่ซ้ำกับระบบอื่น
   - API: พอร์ต 8001 แทน 8000
   - ฐานข้อมูล: พอร์ต 5433 แทน 5432
   - Nginx: พอร์ต 8080

2. **Nginx (03:31-03:33)**
   - สร้าง `nginx/conf.d/sentiment-api.conf` สำหรับ reverse proxy
   - สร้าง `nginx/html/index.html` สำหรับหน้าเว็บเริ่มต้น

3. **ไฟล์สนับสนุนอื่นๆ (03:33-03:35)**
   - สร้าง `.env.example` เพื่อเป็นตัวอย่างการตั้งค่า
   - สร้าง `backup.sh` สำหรับสำรองข้อมูล
   - สร้าง `todo.md` (ไฟล์นี้) เพื่อติดตามงาน

### ทดสอบระบบ (03:40-03:45)

1. **ทดสอบการรัน (03:40-03:42)**
   - รีสตาร์ทระบบผ่าน Docker Compose
   - ตรวจสอบล็อกเพื่อยืนยันว่าระบบเริ่มต้นได้ถูกต้อง
   - ทดสอบเรียก endpoint `/health` ซึ่งทำงานได้ถูกต้อง

2. **ทดสอบ Setup Endpoint (03:42-03:45)**
   - พบปัญหา endpoint `/setup-db` ไม่สามารถเข้าถึงได้ผ่าน Nginx
   - ทดสอบด้วย curl และได้ผลลัพธ์ 404 Not Found
   - ควรแก้ไขการตั้งค่า Nginx ในอนาคต

## 🚀 สิ่งที่ต้องทำต่อ

### 1. แก้ไขปัญหา Nginx Routing (ประมาณ 10 นาที)

- **สาเหตุ**: Nginx ไม่ส่งต่อ request ไปยัง endpoint `/setup-db`
- **วิธีแก้ไข**: ปรับแต่งไฟล์ `nginx/conf.d/sentiment-api.conf` ให้ส่งต่อทุก path อย่างถูกต้อง
- **ตัวอย่างการแก้ไข**:
```nginx
server {
    listen 80;
    server_name _;

    location / {
        proxy_pass http://sentiment-api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # อย่าใช้ location เฉพาะสำหรับ /static/ และ /health เพราะอาจทำให้ paths อื่นเข้าถึงไม่ได้
}
```

### 2. เตรียมงานก่อนขึ้น VM (ประมาณ 30 นาที)

1. **รวบรวมและบีบอัดไฟล์ทั้งหมด**
   - รวมไฟล์ที่แก้ไขทั้งหมดเข้าด้วยกัน
   - สร้างไฟล์ README.md เพื่ออธิบายการติดตั้ง

2. **ทดสอบการติดตั้งบนระบบทดสอบ**
   - ทดสอบขั้นตอนการติดตั้งตามลำดับที่วางแผนไว้
   - ตรวจสอบว่าระบบทำงานได้ตามต้องการ

### 3. การขึ้น VM (ประมาณ 1 ชั่วโมง)

1. **สร้างโฟลเดอร์และโครงสร้างโปรเจค**
   ```bash
   mkdir -p ~/sentiment-api/nginx/{conf.d,html}
   cd ~/sentiment-api
   ```

2. **อัปโหลดไฟล์ไปยัง VM**
   ```bash
   # ทำบนเครื่อง local ของคุณ
   scp -r ./* username@vm-ip:~/sentiment-api/
   ```

3. **ตั้งค่าเบื้องต้น**
   ```bash
   cp .env.example .env
   nano .env  # แก้ไขการตั้งค่าตามต้องการ
   chmod +x backup.sh
   ```

4. **สร้างและเริ่มต้น Containers**
   ```bash
   docker-compose up -d
   ```

5. **ตั้งค่าฐานข้อมูล (หลังจากแก้ไข Nginx)**
   ```bash
   curl http://localhost:8080/setup-db
   ```

### 4. ทดสอบและเก็บข้อมูล (ประมาณ 30 นาที)

1. **ทดสอบระบบทั้งหมดบน VM**
   - เข้าที่ URL: http://vm-ip:8080/
   - ทดสอบการวิเคราะห์ข้อความ
   - ตรวจสอบการแสดงผลบน dashboard

2. **จัดเตรียมการสำรองข้อมูล**
   ```bash
   crontab -e
   # เพิ่มบรรทัดต่อไปนี้
   0 2 * * * cd ~/sentiment-api && ./backup.sh >> backups/backup.log 2>&1
   ```

## 💡 บันทึกเพิ่มเติมและประเด็นที่ต้องพิจารณา

### ประเด็นที่พบระหว่างการทำงาน

1. **ปัญหาความไม่สอดคล้องของโมเดลและฐานข้อมูล**
   - ปัญหา: โมเดลมีฟิลด์ใหม่แต่ฐานข้อมูลยังไม่ได้อัปเดต
   - การแก้ไข: แก้ไขโมเดลชั่วคราวและเพิ่ม migration script
   - ข้อเสนอแนะ: ควรใช้ระบบ migration เช่น Alembic ในอนาคต

2. **ปัญหา Nginx Routing**
   - ปัญหา: endpoint `/setup-db` ไม่สามารถเข้าถึงได้ผ่าน Nginx
   - ต้องแก้ไขการตั้งค่า Nginx ให้ส่งต่อ request อย่างถูกต้อง

3. **การใช้งานในระยะยาว**
   - ควรพิจารณาการเพิ่มระบบ monitoring และ logging เพิ่มเติม
   - ควรพิจารณาการเพิ่มระบบสำรองข้อมูลอัตโนมัติ

### แผนการพัฒนาในอนาคต

1. **ปรับปรุงระบบ Migration**
   - ใช้ Alembic สำหรับจัดการการเปลี่ยนแปลงโครงสร้างฐานข้อมูล
   - เวลาที่คาดว่าจะใช้: 2-3 ชั่วโมง

2. **เพิ่ม HTTPS**
   - ติดตั้ง SSL certificate และตั้งค่า Nginx
   - เวลาที่คาดว่าจะใช้: 1 ชั่วโมง

3. **ปรับปรุง Dashboard**
   - เพิ่มกราฟและการวิเคราะห์เชิงลึก
   - เวลาที่คาดว่าจะใช้: 4-6 ชั่วโมง

4. **เพิ่ม User Authentication**
   - ระบบล็อกอินและการจัดการสิทธิ์
   - เวลาที่คาดว่าจะใช้: 8-10 ชั่วโมง 